{ ERRO2 - constante literal que nunca fecha.
  Precisa de arquivo proprio: o estado 1 engole tudo o que vier pela frente,
  inclusive quebras de linha, e so acusa quando bate no fim do arquivo. }
inicio
    varinicio
        A literal;
    varfim;
    escreva "Digite A:;
    leia A;
fim
